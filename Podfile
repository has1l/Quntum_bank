# Uncomment the next line to define a global platform for your project
platform :ios, '16.0'

use_frameworks!

target 'bank' do
  # WebRTC теперь подключается через Swift Package Manager (SPM)

  target 'bankTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'bankUITests' do
    # Pods for testing
  end

end
